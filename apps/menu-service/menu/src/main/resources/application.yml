server:
  port: 8082

spring:
  application:
    name: menu-service

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mongodb:
    uri: mongodb+srv://kavishka:kavishka2002@kavishka.lyjsvzt.mongodb.net/menu?retryWrites=true&w=majority&appName=ERS

file:
  upload-dir: uploads/menu

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: INFO