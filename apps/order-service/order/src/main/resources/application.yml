server:
  port: 8083

spring:
  application:
    name: order-service

  mongodb:
    uri: mongodb+srv://kavishka:kavishka2002@kavishka.lyjsvzt.mongodb.net/order?retryWrites=true&w=majority&appName=ERS

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: INFO